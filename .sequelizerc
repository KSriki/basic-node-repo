const path = require('path');

module.exports = {
  "config": path.resolve('./server/config', 'config.json'), //database auth etc.
  "models-path": path.resolve('./server/models'),
  "seeders-path": path.resolve('./server/seeders'), //init seed data
  "migrations-path": path.resolve('./server/migrations')
};